mutation SignUp($registerDto: RegisterDto!) {
  SignUp(registerDto: $registerDto) {
    id
    first_name
    last_name
    phone
    email
    bio
    password
    address {
      id
      zip_code
      street
      region
      country
    }
    birth_date
  }
}
